refresh_rate: 30 seconds

appenders:
  requests:
    kind: file
    path: "log/requests.log"
    encoder:
      pattern: "{d} - {m}{n}"

root:
  level: warn
  appenders:
    - stdout

loggers:
  app::requests:
    level: info
    appenders:
      - requests
    additive: false
